apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fiesta-ingress
spec:
  rules:
    - host: bf.josefkolar.cz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web-svc
                port:
                  number: 80
